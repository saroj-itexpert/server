  //GENERATE RANDOM INSTITUTE ID NUMBER
       const generateRandomInstituteNumber = ()=>{
        return Math.floor(10000 + Math.random() * 90000)
       }

export default generateRandomInstituteNumber;